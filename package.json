{
  "name": "agentlit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "poc": "node --env-file=.env --import tsx scripts/poc.ts",
    "gate:phase1": "node --env-file=.env --import tsx scripts/gate-phase1.ts",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
